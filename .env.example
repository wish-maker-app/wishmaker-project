# Supabase (client-safe, anon key)
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJxxx

# NE JAMAIS mettre le service_role_key dans un VITE_ var → exposé au navigateur.
# Le service_role_key ne s'utilise que dans les Edge Functions via Deno.env.

# Stripe (clé publiable test, safe en front)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Admin et Push
VITE_ADMIN_EMAIL=admin@example.com
VITE_VAPID_PUBLIC_KEY=xxx
